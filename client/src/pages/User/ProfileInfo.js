import React from 'react';
import {ProfileWrapper} from "./Profile";

const ProfileInfo = () => {
    return (
        <ProfileWrapper>
            fdfdfd
        </ProfileWrapper>
    );
};

export default ProfileInfo;