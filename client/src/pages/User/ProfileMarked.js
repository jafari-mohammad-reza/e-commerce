import React from 'react';
import {ProfileWrapper} from "./Profile";

const ProfileMarked = () => {
    return (
        <ProfileWrapper>
            dddd
        </ProfileWrapper>
    );
};

export default ProfileMarked;